//change following while-loop into a for-loop and do-while-loop with the same meaning
//<AusdruckA> and <AnweisungB> do not change i
/*

int i;
...
while (( i > 0 ) && ( <AusdruckA> ))
{
<AnweisungB>;
i--;
}

do-while:

if (( i > 0 ) && ( <AusdruckA> ))
{
    do
    {
        <AnweisungB>;
        i--;
    } while(( i > 0 ) && ( <AusdruckA> ));
}

for:

    for(; i > 0 && ( <AusdruckA> ); i--)
    {
        <AnweisungB>;
    }
*/